# Copy this file to .env.local and set it to the ApiUrl printed by `pnpm deploy:backend`
# (no trailing slash). Only needed for `pnpm --filter frontend dev`.
VITE_API_URL=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com
