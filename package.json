{
  "name": "aws-serverless-sample-webapp",
  "version": "1.0.0",
  "private": true,
  "description": "A Persons app: a serverless TypeScript API on AWS and a React website hosted on S3",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm --recursive build",
    "typecheck": "pnpm --recursive typecheck",
    "deploy": "pnpm deploy:backend && pnpm deploy:frontend",
    "deploy:backend": "pnpm --filter backend build && sam build && if [ -f samconfig.toml ]; then sam deploy; else sam deploy --guided; fi",
    "deploy:frontend": "node scripts/deploy-frontend.mjs"
  }
}
